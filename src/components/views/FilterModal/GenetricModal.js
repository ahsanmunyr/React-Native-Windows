import { StyleSheet, Text, View } from 'react-native'
import React, { memo } from 'react'

const GenetricModal = () => {
  return (
    <View>
      <Text>GenetricModal</Text>
    </View>
  )
}

export default memo(GenetricModal)

const styles = StyleSheet.create({})